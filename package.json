{
  "name": "caldav-proxy",
  "version": "1.0.0",
  "description": "To deploy a small node server that will act as a proxy between a http basic auth protected calendar and google",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "albinscode",
  "license": "ISC",
  "dependencies": {
    "caldav": "https://github.com/lambdabaa/dav#v1.8.0",
    "express": "^4.17.2"
  }
}
